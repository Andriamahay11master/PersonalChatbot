# PersonalChatbot
A chat interface that answers questions using a local knowledge base (documents you upload) plus an LLM for natural language interaction. It includes document ingestion, embedding indexing, context-aware responses, and session memory
